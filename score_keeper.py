def add_score(points, current_score):
   """
   Add points to current score and return new total
   Args:
       points (int): Points to add
       current_score (int): Current score
   Returns:
       int: New total score
   TODO:
   - Add error handling for invalid inputs
   - Add points to current_score
   - Return new total
   - Print confirmation message
   """
   pass

def reset_score():
   """
   Reset score to zero
   Returns:
       int: 0 as new score
   TODO:
   - Print reset confirmation message  
   - Return 0
   """
   pass

def display_total(current_score):
   """
   Display and return current score
   Args:
       current_score (int): Current score to display
   Returns:
       int: Current score
   TODO:
   - Print formatted score message
   - Return current_score
   """
   pass

def main():
   """
   Main program loop with menu interface
   TODO:
   - Initialize total_score variable
   - Create welcome message
   - Implement while loop for menu
   - Display menu options (1-4)
   - Add try/except for menu choice input
   - Add if/elif/else for menu options:
       1. Call add_score()
       2. Call reset_score()
       3. Call display_total()
       4. Exit with message
   - Handle invalid menu choices
   """
   pass

# Program entry point
   # TODO: Call main() function
